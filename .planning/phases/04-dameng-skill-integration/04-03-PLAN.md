---
phase: 04-dameng-skill-integration
plan: 03
type: execute
wave: 2
depends_on:
  - 04-02
files_modified:
  - db-cli-skill/src/tools/count.ts
  - db-cli-skill/src/tools/desc.ts
  - db-cli-skill/src/tools/export.ts
  - db-cli-skill/src/tools/import.ts
  - db-cli-skill/src/tools/exec.ts
  - db-cli-skill/src/server/mcp-server.ts
  - db-cli-skill/src/templates/commands.ts
  - db-cli-skill/src/templates/matcher.ts
autonomous: true
requirements:
  - SKILL-01
user_setup: []

must_haves:
  truths:
    - "Claude Code can call count tool to get row count"
    - "Claude Code can call desc tool to view table structure"
    - "Claude Code can call exec tool to execute arbitrary SQL"
    - "Tools delegate to db-cli binary, not re-implement SQL"
  artifacts:
    - path: "db-cli-skill/src/tools/count.ts"
      provides: "count rows tool definition"
      exports: ["registerCountTool"]
    - path: "db-cli-skill/src/templates/commands.ts"
      provides: "Natural language command templates"
      contains: "patterns:"
    - path: "db-cli-skill/src/server/mcp-server.ts"
      provides: "Tool registration and server setup"
      exports: ["createMcpServer"]
  key_links:
    - from: "db-cli-skill/src/tools/count.ts"
      to: "db-cli binary"
      via: "execa spawn"
      pattern: "execa.*dbCliPath"
    - from: "db-cli-skill/src/templates/matcher.ts"
      to: "db-cli-skill/src/templates/commands.ts"
      via: "template iteration"
      pattern: "getAllTemplates"
---

<objective>
Implement the 5 Skill tools (count, desc, export, import, exec) with template-based natural language parsing.

Purpose: Enable Claude Code to understand natural language database requests and convert them to db-cli commands (D-09, D-10).

Output: Working MCP server with all 5 tools registered and functional.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/04-dameng-skill-integration/04-CONTEXT.md
@.planning/phases/04-dameng-skill-integration/04-RESEARCH.md
@db-cli-skill/src/index.ts
</context>

<interfaces>
<!-- Tool interface pattern from RESEARCH.md -->

Expected tool signature:
```typescript
export function registerCountTool(server: McpServer) {
  server.tool('count', 'description', schema, handler);
}
```

Template interface:
```typescript
interface CommandTemplate {
  name: string;
  patterns: RegExp[];
  extractParams: (match: RegExpMatchArray) => Record<string, string>;
  requiredParams?: string[];
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 0: Create template matching system</name>
  <files>db-cli-skill/src/templates/commands.ts, db-cli-skill/src/templates/matcher.ts</files>
  <action>
    Implement template-based natural language parsing (D-09, D-10):

    1. Create commands.ts with CommandTemplate definitions for each tool:
       - count: patterns like "how many rows in table X", "count records from X"
       - desc: "show structure of table X", "describe table X"
       - export: "export table X to file", "backup table X"
       - import: "import SQL file X", "load data from X"
       - exec: "run SQL: ...", "execute: ..."

    2. Create matcher.ts with:
       - getAllTemplates(): returns all CommandTemplate[]
       - matchUserIntent(input: string): MatchResult | null
       - MatchResult includes: tool, params, confidence, missingParams

    3. Each template has patterns (RegExp[]) and extractParams function
    4. Use word boundaries and case-insensitive matching
  </action>
  <verify>
    <automated>cd db-cli-skill && npm run build && node -e "import('./dist/templates/matcher.js').then(m => console.log(m.matchUserIntent('how many rows in users?')))"</automated>
  </verify>
  <done>Template matcher correctly identifies intents and extracts parameters</done>
</task>

<task type="auto">
  <name>Task 1: Implement count tool</name>
  <files>db-cli-skill/src/tools/count.ts</files>
  <action>
    Implement count rows tool per RESEARCH.md Pattern 3:

    1. Define tool schema with Zod:
       - host, database, user, password (required)
       - table (required)
       - where (optional)
       - type (optional, default 'mysql')

    2. Handler function:
       - Build SQL: SELECT COUNT(*) FROM {table} {WHERE clause}
       - Spawn db-cli exec with connection params
       - Capture stdout, parse JSON result
       - Return human-readable count

    3. Error handling:
       - Throw on stderr
       - Handle connection errors gracefully
  </action>
  <verify>
    <automated>cd db-cli-skill && npm run build</automated>
  </verify>
  <done>count tool compiles, exports registerCountTool function</done>
</task>

<task type="auto">
  <name>Task 2: Implement desc tool</name>
  <files>db-cli-skill/src/tools/desc.ts</files>
  <action>
    Implement table description tool:

    1. Define schema: host, database, user, password, table (required), type (optional)
    2. Handler:
       - Spawn db-cli desc -h ... --table={table}
       - Capture and format output
       - Return table structure in readable format

    3. Support optional flags:
       - --indexes: show index information
       - --foreign-keys: show FK constraints
  </action>
  <verify>
    <automated>cd db-cli-skill && npm run build</automated>
  </verify>
  <done>desc tool compiles, exports registerDescTool function</done>
</task>

<task type="auto">
  <name>Task 3: Implement export tool</name>
  <files>db-cli-skill/src/tools/export.ts</files>
  <action>
    Implement data export tool:

    1. Define schema: host, database, user, password, table (optional), query (optional), output (required)
    2. Handler:
       - Validate: either table OR query must be provided
       - Spawn db-cli export -h ... --table={table} --output={file}
       - OR: db-cli export -h ... --query="..." --output={file}
       - Return success message with file path

    3. Handle export format options (insert, ddl, csv)
  </action>
  <verify>
    <automated>cd db-cli-skill && npm run build</automated>
  </verify>
  <done>export tool compiles, exports registerExportTool function</done>
</task>

<task type="auto">
  <name>Task 4: Implement import tool</name>
  <files>db-cli-skill/src/tools/import.ts</files>
  <action>
    Implement SQL file import tool:

    1. Define schema: host, database, user, password, file (required), type (optional)
    2. Handler:
       - Validate file exists
       - Spawn db-cli import -h ... --file={file}
       - Capture output and any errors
       - Return rows imported count or error message
  </action>
  <verify>
    <automated>cd db-cli-skill && npm run build</automated>
  </verify>
  <done>import tool compiles, exports registerImportTool function</done>
</task>

<task type="auto">
  <name>Task 5: Implement exec tool</name>
  <files>db-cli-skill/src/tools/exec.ts</files>
  <action>
    Implement generic SQL execution tool:

    1. Define schema: host, database, user, password, sql (required), type (optional)
    2. Handler:
       - Spawn db-cli exec -h ... -p ... -d ... "{sql}"
       - Capture stdout (JSON for SELECT, rows affected for others)
       - Parse and format output
       - Return result in human-readable format

    3. Safety note in comments: This tool executes arbitrary SQL - use with caution
  </action>
  <verify>
    <automated>cd db-cli-skill && npm run build</automated>
  </verify>
  <done>exec tool compiles, exports registerExecTool function</done>
</task>

<task type="auto">
  <name>Task 6: Register all tools in MCP server</name>
  <files>db-cli-skill/src/server/mcp-server.ts, db-cli-skill/src/index.ts</files>
  <action>
    Wire up all tools to the MCP server:

    1. Create mcp-server.ts with createMcpServer():
       - Initialize McpServer instance
       - Call register[Count|Desc|Export|Import|Exec]Tool(server)
       - Return configured server

    2. Update index.ts:
       - Import createMcpServer
       - Call in main() and connect to transport

    3. Verify all 5 tools are registered (D-08: 精简命令集)
  </action>
  <verify>
    <automated>cd db-cli-skill && npm run build && node dist/index.js</automated>
  </verify>
  <done>MCP server starts with all 5 tools registered</done>
</task>

</tasks>

<verification>
## Verification

1. **Build**: `npm run build` succeeds with no TypeScript errors
2. **Tool registration**: All 5 tools (count, desc, export, import, exec) are registered
3. **Template matching**: matcher.matchUserIntent() correctly parses sample inputs
4. **Server startup**: MCP server starts via stdio transport
</verification>

<success_criteria>
## Success Criteria

- [ ] SKILL-01 satisfied: All 5 tools implemented and registered
- [ ] Template matching (D-09) parses natural language to tool + params
- [ ] Each tool delegates to db-cli binary (not re-implementing SQL)
- [ ] TypeScript compiles without errors
- [ ] Tools accept connection params: host, database, user, password
- [ ] Interactive guidance ready (D-11): missing params detected by matcher
</success_criteria>

<output>
After completion, create `.planning/phases/04-dameng-skill-integration/04-03-SUMMARY.md` documenting:
- Tool implementations and schemas
- Template patterns defined
- Integration test results
</output>
