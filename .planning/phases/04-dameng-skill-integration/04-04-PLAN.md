---
phase: 04-dameng-skill-integration
plan: 04
type: execute
wave: 2
depends_on:
  - 04-02
files_modified:
  - db-cli-skill/src/installer/download.ts
  - db-cli-skill/src/installer/install.ts
  - db-cli-skill/src/cli/binary.ts
  - db-cli-skill/package.json
  - db-cli-skill/README.md
  - .github/workflows/release.yml
autonomous: true
requirements:
  - SKILL-02
user_setup:
  - service: github-personal-access-token
    why: "GitHub API rate limiting for unauthenticated requests"
    env_vars:
      - name: GITHUB_TOKEN
        source: "GitHub Settings -> Developer settings -> Personal access tokens (classic)"

must_haves:
  truths:
    - "Skill installation downloads db-cli binary from GitHub Releases"
    - "Binary downloaded matches current platform (OS/arch)"
    - "Downloaded binary is executable after installation"
  artifacts:
    - path: "db-cli-skill/src/installer/download.ts"
      provides: "GitHub Release download logic"
      exports: ["downloadLatestRelease"]
    - path: ".github/workflows/release.yml"
      provides: "Automated build and release workflow"
      contains: "actions/upload-release-asset"
    - path: "db-cli-skill/src/installer/install.ts"
      provides: "Installation command/entry point"
      exports: ["installSkill"]
  key_links:
    - from: "db-cli-skill/src/installer/download.ts"
      to: "GitHub Releases API"
      via: "@octokit/rest"
      pattern: "octokit.repos.getLatestRelease"
    - from: ".github/workflows/release.yml"
      to: "GitHub Releases"
      via: "actions/upload-release-asset"
      pattern: "upload-release-asset"
---

<objective>
Implement automatic db-cli binary download from GitHub Releases and create CI/CD workflow for automated builds.

Purpose: Enable one-command Skill installation with automatic db-cli binary download (D-07, SKILL-02) and automated releases (D-13).

Output: Working installer and GitHub Actions workflow for cross-platform builds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/04-dameng-skill-integration/04-CONTEXT.md
@.planning/phases/04-dameng-skill-integration/04-RESEARCH.md
@db-cli-skill/package.json
</context>

<interfaces>
<!-- GitHub Release download pattern from RESEARCH.md -->

From RESEARCH.md Pattern 5:
```typescript
async function downloadLatestRelease(owner: string, repo: string): Promise<string> {
  const octokit = new Octokit({ auth: process.env.GITHUB_TOKEN });
  const { data: release } = await octokit.repos.getLatestRelease({ owner, repo });
  const asset = release.assets.find(a => a.name.endsWith(platformSuffix));
  const downloadUrl = asset.browser_download_url;
  // Download and save to binary path
}
```

Platform detection:
```typescript
function getPlatformSuffix(): string {
  // Returns: '-windows-amd64.exe', '-darwin-arm64', '-linux-amd64', etc.
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 0: Add GitHub API and archive dependencies</name>
  <files>db-cli-skill/package.json</files>
  <action>
    Add dependencies for GitHub release download per RESEARCH.md Standard Stack:

    1. Add @octokit/rest for GitHub API client
    2. Add extract-zip or tar-stream for archive extraction
    3. Add node-fetch for binary download (or use built-in fetch)

    Run npm install to update package-lock.json
  </action>
  <verify>
    <automated>cd db-cli-skill && npm install && npm list @octokit/rest</automated>
  </verify>
  <done>Dependencies installed, package.json updated</done>
</task>

<task type="auto">
  <name>Task 1: Implement platform detection utility</name>
  <files>db-cli-skill/src/utils/platform.ts</files>
  <action>
    Create cross-platform detection module:

    1. Create getPlatformInfo() returning { os: string, arch: string, ext: string }
    2. Create getPlatformSuffix() returning release asset suffix:
       - windows/amd64: '-windows-amd64.exe'
       - windows/arm64: '-windows-arm64.exe'
       - darwin/amd64: '-darwin-amd64'
       - darwin/arm64: '-darwin-arm64'
       - linux/amd64: '-linux-amd64'
       - linux/arm64: '-linux-arm64'

    3. Handle edge cases (unknown platform throws error)
  </action>
  <verify>
    <automated>cd db-cli-skill && npm run build && node -e "import('./dist/utils/platform.js').then(m => console.log(m.getPlatformSuffix()))"</automated>
  </verify>
  <done>Platform detection works, returns correct suffix for current OS</done>
</task>

<task type="auto">
  <name>Task 2: Implement GitHub Release downloader</name>
  <files>db-cli-skill/src/installer/download.ts</files>
  <action>
    Implement downloadLatestRelease following RESEARCH.md Pattern 5:

    1. Create Octokit instance with optional GITHUB_TOKEN auth
    2. Fetch latest release from xiaokexiang/db-cli
    3. Find asset matching current platform suffix
    4. Download binary from browser_download_url
    5. Save to temporary location
    6. Return downloaded file path

    Error handling:
    - No release found: throw "No release found"
    - No matching asset: throw "No binary for platform: {platform}"
    - Download failed: throw with original error
  </action>
  <verify>
    <automated>cd db-cli-skill && npm run build</automated>
  </verify>
  <done>Download module compiles, exports downloadLatestRelease function</done>
</task>

<task type="auto">
  <name>Task 3: Implement binary installer</name>
  <files>db-cli-skill/src/installer/install.ts</files>
  <action>
    Create installation orchestration module:

    1. Create installSkill() function:
       - Call downloadLatestRelease()
       - Move binary to getBinaryPath() location
       - Set executable permissions (chmod 755 on Unix)
       - Create parent directories if needed

    2. Create verifyInstallation() function:
       - Check binary exists at expected path
       - Check binary is executable
       - Return boolean

    3. Create uninstallSkill() function:
       - Remove binary from local path
       - Remove empty directories
  </action>
  <verify>
    <automated>cd db-cli-skill && npm run build</automated>
  </verify>
  <done>Installer module compiles, exports install/verify/uninstall functions</done>
</task>

<task type="auto">
  <name>Task 4: Create CLI installation command</name>
  <files>db-cli-skill/src/cli/install-cmd.ts</files>
  <action>
    Create user-facing installation CLI:

    1. Create command: `npx db-cli-skill install`
    2. Flow:
       - Check if binary already exists (offer to overwrite)
       - Download latest release
       - Install to local path
       - Verify installation
       - Print success message with usage instructions

    3. Add flags:
       - --force: overwrite existing without prompt
       - --version: install specific version (not latest)

    4. Print Claude Code configuration after install
  </action>
  <verify>
    <automated>cd db-cli-skill && npm run build && node dist/cli/install-cmd.js --help</automated>
  </verify>
  <done>Install CLI command works, shows help and options</done>
</task>

<task type="auto">
  <name>Task 5: Create GitHub Actions release workflow</name>
  <files>.github/workflows/release.yml</files>
  <action>
    Create CI/CD workflow per D-13 (automated builds and releases):

    1. Trigger: on tag push (v*)
    2. Matrix builds for all platforms:
       - windows/amd64, windows/arm64
       - darwin/amd64, darwin/arm64
       - linux/amd64, linux/arm64

    3. Build steps:
       - Setup Go
       - go mod download
       - CGO_ENABLED=0 go build (for pure Go MySQL build)
       - For Dameng: document CGO requirements in release notes

    4. Release steps:
       - Create GitHub Release from tag
       - Upload all binaries as release assets
       - Generate changelog from commits

    Note: Dameng CGO builds may require separate workflow with client libraries installed.
  </action>
  <verify>
    <automated>test -f .github/workflows/release.yml && head -30 .github/workflows/release.yml</automated>
  </verify>
  <done>release.yml created with matrix builds and release asset upload</done>
</task>

<task type="auto">
  <name>Task 6: Update README with installation instructions</name>
  <files>db-cli-skill/README.md</files>
  <action>
    Update documentation with SKILL-02 installation flow:

    1. Quick start section:
       - npm install -g @xiaokexiang/db-cli-skill
       - db-cli-skill install (downloads binary)
       - Configure Claude Code MCP

    2. Claude Code configuration:
       ```json
       {
         "mcpServers": {
           "db-cli-skill": {
             "command": "npx",
             "args": ["-y", "db-cli-skill"]
           }
         }
       }
       ```

    3. Manual installation option:
       - Download db-cli from GitHub Releases
       - Place in PATH
       - Install Skill separately

    4. GITHUB_TOKEN note for rate limiting
  </action>
  <verify>
    <automated>test -f db-cli-skill/README.md && grep -A5 "install" db-cli-skill/README.md | head -20</automated>
  </verify>
  <done>README updated with complete installation instructions</done>
</task>

</tasks>

<verification>
## Verification

1. **Build verification**: `npm run build` succeeds for db-cli-skill
2. **Platform detection**: getPlatformSuffix() returns correct value for current OS
3. **Download logic**: downloadLatestRelease compiles without errors
4. **Workflow validation**: .github/workflows/release.yml is valid YAML
5. **Documentation**: README contains installation instructions
</verification>

<success_criteria>
## Success Criteria

- [ ] SKILL-02 satisfied: Skill installation downloads db-cli from GitHub Releases
- [ ] D-07 satisfied: Automatic download on install command
- [ ] D-13 satisfied: GitHub Actions workflow for automated builds
- [ ] Cross-platform builds: All 6 platform targets configured
- [ ] Platform detection works correctly
- [ ] Binary installation sets proper permissions
- [ ] README documents installation process
- [ ] GITHUB_TOKEN documented for rate limiting avoidance
</success_criteria>

<output>
After completion, create `.planning/phases/04-dameng-skill-integration/04-04-SUMMARY.md` documenting:
- Release workflow configuration
- Supported platforms and build matrix
- Installation flow
- Known issues (e.g., Dameng CGO builds)
</output>
