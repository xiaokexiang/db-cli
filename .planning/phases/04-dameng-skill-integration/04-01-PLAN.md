---
phase: 04-dameng-skill-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/database/connection.go
  - internal/database/connection_dameng_test.go
  - go.mod
  - go.sum
autonomous: true
requirements:
  - CONN-03
user_setup: []

must_haves:
  truths:
    - "User can connect to Dameng database using -t dameng flag"
    - "Dameng connection uses same flag interface as MySQL (-h, -P, -u, -p, -d)"
    - "SQL execution against Dameng returns results in JSON format"
  artifacts:
    - path: "internal/database/connection.go"
      provides: "Dameng DSN building and GORM connection"
      contains: "case \"dameng\""
    - path: "internal/database/connection_dameng_test.go"
      provides: "Dameng connection tests"
      exports: ["TestDamengConnection"]
  key_links:
    - from: "cmd/exec.go"
      to: "internal/database/connection.go"
      via: "database.OpenConnection(cfg)"
      pattern: "OpenConnection.*cfg"
---

<objective>
Implement Dameng database driver integration with GORM, extending the existing connection factory to support `-t dameng` flag.

Purpose: Enable users to connect to Dameng DM8 databases using the same CLI interface as MySQL (D-04, D-05).

Output: Working Dameng connection layer with tests, ready for Skill integration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/04-dameng-skill-integration/04-CONTEXT.md
@.planning/phases/04-dameng-skill-integration/04-RESEARCH.md
@internal/database/connection.go
</context>

<interfaces>
<!-- Existing connection interface from connection.go -->

From internal/database/connection.go:
```go
// ConnectionConfig holds the database connection parameters
type ConnectionConfig struct {
    Host     string
    Port     int
    User     string
    Password string
    Database string
    DBType   string // "mysql" or "dameng"
}

// BuildDSN builds a Data Source Name from the connection config
func BuildDSN(cfg ConnectionConfig) (string, error)

// OpenConnection opens a GORM database connection
func OpenConnection(cfg ConnectionConfig) (*gorm.DB, error)

// CloseConnection closes the database connection
func CloseConnection(db *gorm.DB) error
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0: Create Dameng connection test scaffold</name>
  <files>internal/database/connection_dameng_test.go</files>
  <action>
    Create test file for Dameng connection with the following test cases:

    Test cases in &lt;behavior&gt;:
    - TestDamengDSN_BuildsCorrectly: DSN format for Dameng with default port 5236
    - TestDamengDSN_CustomPort: DSN with custom port
    - TestDamengDSN_ValidationErrors: Missing host/user/database returns error
    - TestDamengConnection_Integration:实际连接测试 (requires Dameng server, skip if unavailable)

    Create failing tests first per TDD pattern.
  </action>
  <verify>
    <automated>go test ./internal/database -run TestDameng -v</automated>
  </verify>
  <done>Test file created with failing tests for Dameng DSN building</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Research and select Dameng driver</name>
  <files>go.mod</files>
  <action>
    Per D-01 (优先纯 Go 方案), use Context7 MCP to research Dameng Go drivers:

    1. Query for "dameng dm8 go driver" to find available options
    2. Evaluate: gorm.io/driver/dm (official), github.com/cherishlee/dm-go-driver, alternatives
    3. Select driver based on: pure Go preference, GORM compatibility, maintenance status
    4. Add selected driver to go.mod

    If pure Go driver unavailable, select CGO-based driver and document in code comments.
    Reference: 04-RESEARCH.md Appendix for known options.
  </action>
  <verify>
    <automated>go mod tidy && go list -m all | grep -i dm</automated>
  </verify>
  <done>Dameng driver added to go.mod, import path documented in connection.go</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Dameng DSN builder</name>
  <files>internal/database/connection.go</files>
  <action>
    Extend BuildDSN function to support "dameng" type:

    1. Add case "dameng" to the switch statement (currently returns error at line 59)
    2. Build Dameng DSN with format per driver requirements:
       - Default port: 5236 (not 3306 like MySQL)
       - Format likely: "user:password@tcp(host:port)/database" or driver-specific
    3. Add validation for Dameng-specific requirements
    4. Return clear error for unsupported features

    The DSN format must match what the selected driver expects (verify via Context7).
  </action>
  <verify>
    <automated>go test ./internal/database -run TestDamengDSN -v</automated>
  </verify>
  <done>Dameng DSN builds correctly, tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement Dameng GORM connection</name>
  <files>internal/database/connection.go</files>
  <action>
    Extend OpenConnection function to support Dameng:

    1. Add dameng case to switch statement
    2. Import selected Dameng driver (side-effect import with `_` if required)
    3. Call gorm.Open with Dameng dialect
    4. Handle CGO requirements if applicable

    Pattern from RESEARCH.md:
    ```go
    case "dameng":
        return gorm.Open(dameng.Open(dsn), &gorm.Config{})
    ```

    Add code comments documenting any CGO requirements (D-02).
  </action>
  <verify>
    <automated>go test ./internal/database -run TestDamengConnection -v</automated>
  </verify>
  <done>OpenConnection supports dameng type, integration tests pass (or skip gracefully)</done>
</task>

<task type="auto">
  <name>Task 4: Verify end-to-end Dameng execution</name>
  <files>cmd/exec.go</files>
  <action>
    Verify existing exec command works with -t dameng without modification:

    1. The exec command already uses database.OpenConnection(cfg)
    2. No code changes needed if connection layer is correct
    3. Add a manual verification note in comments

    Document the expected usage:
    `db-cli exec -t dameng -h <host> -u <user> -p <password> -d <database> 'SELECT * FROM table'`
  </action>
  <verify>
    <automated>go build -o db-cli.exe && db-cli.exe exec -t dameng --help</automated>
  </verify>
  <done>Build succeeds, -t flag accepts dameng value, help text updated</done>
</task>

</tasks>

<verification>
## Verification

1. **Build verification**: `go build ./...` succeeds
2. **Unit tests**: All Dameng DSN tests pass
3. **Integration tests**: Dameng connection test passes (if Dameng server available) or skips gracefully
4. **CLI verification**: `db-cli exec -t dameng -h ...` accepts the flag without error
</verification>

<success_criteria>
## Success Criteria

- [ ] CONN-03 requirement satisfied: Dameng database connection works via GORM
- [ ] Same flag interface as MySQL: `-h`, `-P`, `-u`, `-p`, `-d`, `-t dameng`
- [ ] Default Dameng port 5236 used when not specified
- [ ] Tests exist for Dameng DSN building
- [ ] Driver selection documented (pure Go vs CGO)
- [ ] No breaking changes to existing MySQL functionality
</success_criteria>

<output>
After completion, create `.planning/phases/04-dameng-skill-integration/04-01-SUMMARY.md` documenting:
- Selected Dameng driver and rationale
- DSN format used
- Any CGO build requirements
- Test results
</output>
