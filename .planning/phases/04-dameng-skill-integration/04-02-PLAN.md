---
phase: 04-dameng-skill-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - db-cli-skill/package.json
  - db-cli-skill/tsconfig.json
  - db-cli-skill/src/index.ts
  - db-cli-skill/src/server/mcp-server.ts
  - db-cli-skill/README.md
autonomous: true
requirements:
  - SKILL-01
user_setup:
  - service: nodejs
    why: "Skill MCP server requires Node.js runtime"
    env_vars: []
  - service: github-personal-access-token
    why: "GitHub API for release downloads (SKILL-02)"
    env_vars:
      - name: GITHUB_TOKEN
        source: "GitHub Settings -> Developer settings -> Personal access tokens"

must_haves:
  truths:
    - "Skill can be installed as a Claude Code MCP server"
    - "Skill exposes 5 tools: count, desc, export, import, exec"
    - "Each tool accepts connection parameters and delegates to db-cli binary"
  artifacts:
    - path: "db-cli-skill/package.json"
      provides: "NPM package configuration with MCP dependencies"
      contains: "@modelcontextprotocol/sdk"
    - path: "db-cli-skill/src/index.ts"
      provides: "MCP server entry point"
      exports: ["main"]
    - path: "db-cli-skill/README.md"
      provides: "Installation and usage documentation"
      contains: "claude"
  key_links:
    - from: "db-cli-skill/src/index.ts"
      to: "db-cli binary"
      via: "child_process.execa"
      pattern: "execa.*db-cli"
---

<objective>
Create the db-cli-skill MCP server foundation with project structure, dependencies, and server entry point.

Purpose: Enable Claude Code to call db-cli commands through a Model Context Protocol server (D-06, SKILL-01).

Output: Working MCP server skeleton that can be extended with tools.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/04-dameng-skill-integration/04-CONTEXT.md
@.planning/phases/04-dameng-skill-integration/04-RESEARCH.md
</context>

<interfaces>
<!-- MCP Server patterns from RESEARCH.md -->

From 04-RESEARCH.md Pattern 3:
```typescript
// Tool definition pattern
server.tool(
  'count',
  'Count rows in a database table',
  {
    host: z.string().describe('Database host IP'),
    database: z.string().describe('Database name'),
    user: z.string().describe('Database user'),
    password: z.string().describe('Database password'),
    table: z.string().describe('Table name'),
    where: z.string().optional().describe('WHERE clause'),
  },
  async ({ host, database, user, password, table, where }) => {
    const { stdout, stderr } = await execa(dbCliPath, [...]);
    return { content: [{ type: 'text', text: stdout }] };
  }
);
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 0: Initialize db-cli-skill TypeScript project</name>
  <files>db-cli-skill/package.json, db-cli-skill/tsconfig.json</files>
  <action>
    Create new directory structure for Skill (D-06: separate repository):

    1. Create db-cli-skill/ directory
    2. Initialize package.json with:
       - name: @xiaokexiang/db-cli-skill
       - type: module
       - scripts: build, start, dev
       - dependencies: @modelcontextprotocol/sdk, zod (for schema validation)
       - devDependencies: typescript, @types/node, tsx (for dev execution)

    3. Create tsconfig.json with:
       - target: ES2022
       - module: NodeNext
       - outDir: ./dist
       - rootDir: ./src
       - strict: true

    Use RESEARCH.md Standard Stack for MCP server SDK.
  </action>
  <verify>
    <automated>cd db-cli-skill && npm install && npm run build</automated>
  </verify>
  <done>TypeScript project initialized, dependencies installed, build succeeds</done>
</task>

<task type="auto">
  <name>Task 1: Create MCP server entry point</name>
  <files>db-cli-skill/src/index.ts</files>
  <action>
    Create main entry point following RESEARCH.md Pattern "MCP Server Entry Point":

    1. Import McpServer and StdioServerTransport
    2. Create server instance with name='db-cli-skill', version='1.0.0'
    3. Set up main() function that:
       - Creates MCP server
       - Connects to StdioServerTransport
       - Handles errors
    4. Export main function for modularity

    This is the skeleton - tools will be added in Plan 03.
  </action>
  <verify>
    <automated>cd db-cli-skill && npm run build && node dist/index.js --help</automated>
  </verify>
  <done>Server entry point compiles and runs (exits cleanly or shows usage)</done>
</task>

<task type="auto">
  <name>Task 2: Create binary path management module</name>
  <files>db-cli-skill/src/utils/binary-path.ts</files>
  <action>
    Create module to manage db-cli binary location:

    1. Create getBinaryPath() function that:
       - Returns platform-specific path: ~/.db-cli/bin/db-cli or %APPDATA%\.db-cli\bin\db-cli.exe
       - Checks if binary exists and is executable
       - Creates directory if needed

    2. Create ensureBinaryExists() function that:
       - Returns true if binary exists at expected path
       - Returns false if missing (triggers download in Plan 04)

    3. Handle cross-platform paths using path.join and os.homedir()
  </action>
  <verify>
    <automated>cd db-cli-skill && npm run build && node -e "import('./dist/utils/binary-path.js').then(m => console.log(m.getBinaryPath()))"</automated>
  </verify>
  <done>Binary path module exports getBinaryPath and ensureBinaryExists functions</done>
</task>

<task type="auto">
  <name>Task 3: Create README with Claude Code installation instructions</name>
  <files>db-cli-skill/README.md</files>
  <action>
    Create documentation for Skill installation and usage:

    1. Title: db-cli-skill - Claude Code Skill for Database Operations
    2. Features section: count, desc, export, import, exec commands (D-08)
    3. Installation section:
       - Prerequisites: Node.js, db-cli binary
       - Claude Code MCP configuration
       - GitHub Releases download instructions
    4. Usage examples:
       - Natural language examples for each command
       - Connection parameter examples
    5. Configuration: GITHUB_TOKEN for release downloads
  </action>
  <verify>
    <automated>test -f db-cli-skill/README.md && head -20 db-cli-skill/README.md</automated>
  </verify>
  <done>README.md exists with installation instructions and usage examples</done>
</task>

</tasks>

<verification>
## Verification

1. **Build verification**: `npm run build` succeeds in db-cli-skill/
2. **Server startup**: MCP server starts without errors
3. **Binary path**: getBinaryPath() returns valid platform-specific path
4. **Documentation**: README contains Claude Code configuration instructions
</verification>

<success_criteria>
## Success Criteria

- [ ] SKILL-01 foundation: MCP server project structure complete
- [ ] Server can start and connect via StdioServerTransport
- [ ] Binary path management works cross-platform
- [ ] README documents D-06 (separate repo), D-08 (5 commands), D-12 (manual update)
- [ ] TypeScript compiles without errors
- [ ] Project ready for tool implementation in Plan 03
</success_criteria>

<output>
After completion, create `.planning/phases/04-dameng-skill-integration/04-02-SUMMARY.md` documenting:
- Project structure created
- MCP server SDK version used
- Binary path conventions
- Installation instructions
</output>
