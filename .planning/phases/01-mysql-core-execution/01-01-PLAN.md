---
phase: 01-mysql-core-execution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [go.mod, go.sum, .gitignore, Makefile]
autonomous: true
requirements: [PLATFORM-01, PLATFORM-02]
must_haves:
  truths:
    - "Go module initialized with correct dependencies"
    - "Build system supports all 6 target platforms"
  artifacts:
    - path: go.mod
      provides: Module definition and dependencies
      contains: "github.com/spf13/cobra, gorm.io/gorm, github.com/go-sql-driver/mysql"
    - path: go.sum
      provides: Dependency checksums
    - path: .gitignore
      provides: Git exclusion rules
    - path: Makefile
      provides: Cross-platform build targets
  key_links:
    - from: Makefile
      to: go build
      via: GOOS/GOARCH environment variables
      pattern: "GOOS=.*GOARCH=.*go build"
---

# Phase 1: MySQL Core Execution - Plan 01

<objective>
Initialize Go module with all required dependencies and build configuration

Purpose: Establish project foundation with correct versions of Cobra, GORM, and MySQL driver
Output: go.mod with dependencies, .gitignore, cross-platform Makefile
</objective>

<execution_context>
@C:/Users/xiaokexiang/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/xiaokexiang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-mysql-core-execution/01-CONTEXT.md
</context>

<interfaces>
<!-- No existing interfaces - greenfield project -->
</interfaces>

<tasks>

<task type="auto">
<name>Task 1: Initialize Go module and add dependencies</name>
<read_first>
- CLAUDE.md (project constraints, tech stack)
- .planning/phases/01-mysql-core-execution/01-CONTEXT.md (decisions D-08, D-09)
</read_first>
<action>
Run the following commands in project root:

1. Initialize Go module:
   `go mod init github.com/xiaokexiang/db-cli`

2. Add Cobra CLI framework (D-01):
   `go get github.com/spf13/cobra@v1.10.2`

3. Add GORM ORM layer (D-09):
   `go get gorm.io/gorm@latest`

4. Add MySQL driver (D-08):
   `go get github.com/go-sql-driver/mysql@v1.9.3`

5. Tidy dependencies:
   `go mod tidy`
</action>
<acceptance_criteria>
- go.mod exists with module name "github.com/xiaokexiang/db-cli"
- go.mod contains "github.com/spf13/cobra v1.10.2"
- go.mod contains "gorm.io/gorm"
- go.mod contains "github.com/go-sql-driver/mysql v1.9.3"
- go.sum exists and is non-empty
- `go mod verify` succeeds
</acceptance_criteria>
<verify>
<automated>go mod verify</automated>
</verify>
<done>
go.mod and go.sum created with all required dependencies at correct versions
</done>
</task>

<task type="auto">
<name>Task 2: Create .gitignore and Makefile</name>
<read_first>
- CLAUDE.md (build strategy: cross-platform, single binary)
- .planning/phases/01-mysql-core-execution/01-CONTEXT.md (decisions D-24, D-25)
</read_first>
<action>
Create .gitignore with these entries:
```
# Binaries
db-cli
db-cli.exe
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test results
*.test
*.out

# Go workspace
go.work

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Build artifacts
build/
dist/
```

Create Makefile with these targets:
```makefile
.PHONY: build clean test windows macos linux

BINARY_NAME=db-cli
VERSION=1.0.0

build:
	go build -o $(BINARY_NAME) ./cmd

clean:
	rm -f $(BINARY_NAME)
	rm -rf build/

windows:
	GOOS=windows GOARCH=amd64 go build -o build/$(BINARY_NAME)-windows-amd64.exe ./cmd
	GOOS=windows GOARCH=arm64 go build -o build/$(BINARY_NAME)-windows-arm64.exe ./cmd

macos:
	GOOS=darwin GOARCH=amd64 go build -o build/$(BINARY_NAME)-darwin-amd64 ./cmd
	GOOS=darwin GOARCH=arm64 go build -o build/$(BINARY_NAME)-darwin-arm64 ./cmd

linux:
	GOOS=linux GOARCH=amd64 go build -o build/$(BINARY_NAME)-linux-amd64 ./cmd
	GOOS=linux GOARCH=arm64 go build -o build/$(BINARY_NAME)-linux-arm64 ./cmd

test:
	go test -v ./...
```
</action>
<acceptance_criteria>
- .gitignore exists
- .gitignore contains "db-cli", "*.exe", "*.so", "*.dylib", "build/", "dist/"
- Makefile exists
- Makefile contains targets: build, clean, windows, macos, linux, test
- Makefile contains GOOS/GOARCH combinations for all 6 platforms
</acceptance_criteria>
<verify>
<automated>test -f .gitignore && test -f Makefile && grep -q "GOOS=windows" Makefile && grep -q "GOOS=darwin" Makefile && grep -q "GOOS=linux" Makefile</automated>
</verify>
<done>
.gitignore excludes binaries and build artifacts; Makefile supports cross-platform builds for all 6 target platforms
</done>
</task>

</tasks>

<verification>
- `go mod verify` succeeds
- `make build` compiles without errors (after subsequent plans implement cmd/)
- All 6 platform builds succeed via make targets
</verification>

<success_criteria>
- Dependencies installed at correct versions (Cobra v1.10.2, MySQL driver v1.9.3)
- Clean module state (go mod tidy completes without changes)
- Build system ready for cross-platform compilation
</success_criteria>

<output>
After completion, create .planning/phases/01-mysql-core-execution/01-01-SUMMARY.md with:
- Dependency versions confirmed
- Any build issues encountered
- Platform-specific notes
</output>
