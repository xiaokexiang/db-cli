---
phase: 01-mysql-core-execution
plan: 04
type: execute
wave: 4
depends_on: [01, 02, 03]
files_modified: [cmd/exec.go, cmd/exec_test.go, internal/output/json.go]
autonomous: true
requirements: [EXEC-01, EXEC-02, DQL-01, IO-01]
must_haves:
  truths:
    - "User can execute single SQL: `db-cli exec 'SELECT * FROM table'`"
    - "User can execute SQL file: `db-cli exec --file=script.sql`"
    - "SELECT results output as JSON array"
    - "File execution processes statements sequentially"
  artifacts:
    - path: cmd/exec.go
      provides: Exec command implementation
      exports: [execCmd]
    - path: internal/output/json.go
      provides: JSON formatting
      exports: [ToJSON]
  key_links:
    - from: execCmd.RunE
      to: OpenConnection
      via: cfg global
      pattern: "OpenConnection\(cfg\)"
    - from: execCmd.RunE
      to: ToJSON
      via: output formatting
      pattern: "output\.ToJSON\("
---

# Phase 1: MySQL Core Execution - Plan 04

<objective>
Implement exec command for SQL execution with JSON output

Purpose: Core functionality - execute SQL statements and files
Output: exec command, SQL file parser, JSON formatter
</objective>

<execution_context>
@C:/Users/xiaokexiang/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/xiaokexiang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-mysql-core-execution/01-CONTEXT.md
</context>

<interfaces>
<!-- From Plan 02 - connection layer -->
```go
type ConnectionConfig struct { ... }
func OpenConnection(cfg ConnectionConfig) (*gorm.DB, error)
func CloseConnection(db *gorm.DB) error
```

<!-- From Plan 03 - root command -->
```go
var cfg database.ConnectionConfig  // global in cmd/root.go
```
</interfaces>

<tasks>

<task type="auto">
<name>Task 1: Create JSON output formatter</name>
<read_first>
- .planning/phases/01-mysql-core-execution/01-CONTEXT.md (D-21 - JSON default output)
- .planning/REQUIREMENTS.md (DQL-01 - JSON array output)
</read_first>
<action>
Create internal/output/json.go:

1. Package: package output

2. Imports:
   - "encoding/json"
   - "fmt"
   - "gorm.io/gorm"

3. func ToJSON(rows *gorm.DB) ([]byte, error):
   - Get column names from rows.Columns()
   - Iterate through result set
   - Build []map[string]interface{} for each row
   - Marshal to JSON array
   - Return json.Marshal(result)

4. func ScanRows(rows *gorm.Rows) ([]map[string]interface{}, error):
   - Helper to scan each row into map
   - Handle NULL values properly
</action>
<acceptance_criteria>
- internal/output/json.go exists
- File contains "func ToJSON" that accepts gorm result
- File contains "func ScanRows" helper
- Output is JSON array format: [{...}, {...}]
- NULL database values handled (not causing panic)
</acceptance_criteria>
<verify>
<automated>test -f internal/output/json.go && grep -q "func ToJSON" internal/output/json.go && grep -q "json.Marshal" internal/output/json.go</automated>
</verify>
<done>
JSON formatter converts GORM results to JSON array
</done>
</task>

<task type="auto" tdd="true">
<name>Task 2: Create exec command skeleton and tests</name>
<read_first>
- cmd/root.go (global cfg variable, Cobra patterns)
- .planning/phases/01-mysql-core-execution/01-CONTEXT.md (D-13 to D-15)
- .planning/REQUIREMENTS.md (EXEC-01, EXEC-02)
</read_first>
<action>
Create cmd/exec_test.go with test expectations:

1. TestExecCommand_SingleSQL:
   - Verify exec command exists
   - Accepts SQL as positional argument

2. TestExecCommand_SQLFile:
   - --file flag accepted
   - File path validation

3. TestExecCommand_OutputFormat:
   - --format flag accepts: json, table, csv
   - Default is json

Then create cmd/exec.go skeleton (RED phase - minimal implementation):
- var execCmd = &cobra.Command{...}
- Use: "exec [flags] '<SQL>'"
- Flags: --file, --format, --autocommit
- RunE: stub that returns "not implemented"
</action>
<acceptance_criteria>
- cmd/exec_test.go exists with test functions
- cmd/exec.go exists with execCmd definition
- execCmd has Use, Short, Long, RunE fields
- Flags defined: file, format, autocommit
</acceptance_criteria>
<verify>
<automated>test -f cmd/exec.go && grep -q "var execCmd" cmd/exec.go && grep -q "\-\-file" cmd/exec.go</automated>
</verify>
<done>
Exec command skeleton with tests defined
</done>
</task>

<task type="auto" tdd="true">
<name>Task 3: Implement single SQL execution (GREEN)</name>
<read_first>
- cmd/exec.go (skeleton from Task 2)
- internal/database/connection.go (OpenConnection, CloseConnection)
- internal/output/json.go (ToJSON formatter)
</read_first>
<action>
Implement execCmd.RunE in cmd/exec.go:

1. Parse flags:
   - Get SQL from args (args[0] if not --file)
   - Get --file path
   - Get --format (default "json")
   - Get --autocommit (default true)

2. Validate:
   - Error if both SQL arg and --file provided
   - Error if neither provided

3. If single SQL (no --file):
   - Open connection using global cfg
   - defer CloseConnection
   - Execute: db.Exec(sql) for non-SELECT, db.Raw(sql) for SELECT
   - If SELECT: format output with ToJSON
   - Print output to stdout

4. Handle errors:
   - Return error from RunE for non-zero exit
</action>
<acceptance_criteria>
- execCmd.RunE handles single SQL argument
- execCmd.RunE handles --file flag
- Connection opened and closed properly (defer pattern)
- SELECT queries output as JSON
- Non-SELECT queries show rows affected
- Error returned on failure (non-zero exit)
</acceptance_criteria>
<verify>
<automated>go build -o db-cli . && ./db-cli exec --help</automated>
</verify>
<done>
Single SQL execution works: `db-cli exec -h ... 'SELECT 1'` returns JSON
</done>
</task>

<task type="auto" tdd="true">
<name>Task 4: Implement SQL file execution (GREEN)</name>
<read_first>
- cmd/exec.go (single SQL from Task 3)
- .planning/REQUIREMENTS.md (EXEC-02 - SQL file execution)
</read_first>
<action>
Add SQL file execution to execCmd.RunE:

1. Read file: os.ReadFile(filePath)

2. Parse statements:
   - Split by semicolon (;)
   - Trim whitespace
   - Skip empty statements

3. For each statement:
   - If --autocommit=true: exec each in its own transaction
   - If --autocommit=false: wrap all in single transaction
   - BEGIN before first, COMMIT after last, ROLLBACK on error

4. Output:
   - Only output for last SELECT if multiple statements
   - Or output "X statements executed" summary
</action>
<acceptance_criteria>
- SQL file parsed into individual statements
- Statements split by semicolon
- Empty statements skipped
- Each statement executed sequentially
- File execution shows progress or summary
</acceptance_criteria>
<verify>
<automated>echo "SELECT 1; SELECT 2;" > /tmp/test.sql && ./db-cli exec --file=/tmp/test.sql -h localhost -u root -p test -d test 2>&1 | head -10</automated>
</verify>
<done>
SQL file execution works: `db-cli exec --file=script.sql` executes all statements
</done>
</task>

</tasks>

<verification>
- `db-cli exec 'SELECT 1'` returns JSON: [{"1":1}]
- `db-cli exec --file=test.sql` executes all statements in file
- Connection properly opened and closed
- Output is valid JSON (parseable)
</verification>

<success_criteria>
- Single SQL execution functional
- SQL file execution functional
- JSON output default working
- File + CLI argument mutual exclusion enforced
</success_criteria>

<output>
After completion, create .planning/phases/01-mysql-core-execution/01-04-SUMMARY.md with:
- Exec command implementation notes
- SQL parsing approach
- JSON output examples
</output>
