---
phase: 01-mysql-core-execution
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [internal/database/connection.go, internal/database/connection_test.go]
autonomous: true
requirements: [CONN-01, CONN-02]
must_haves:
  truths:
    - "Connection config can be built from individual parameters"
    - "MySQL DSN is correctly formatted with all required parameters"
    - "GORM connection opens successfully with valid credentials"
  artifacts:
    - path: internal/database/connection.go
      provides: Connection logic
      exports: [ConnectionConfig, BuildDSN, OpenConnection, CloseConnection]
    - path: internal/database/connection_test.go
      provides: Unit tests for connection logic
  key_links:
    - from: OpenConnection
      to: gorm.Open
      via: mysql.Open(dsn)
      pattern: "gorm\.Open\(mysql\.Open"
---

# Phase 1: MySQL Core Execution - Plan 02

<objective>
Create database connection layer with GORM supporting MySQL connections via flag parameters

Purpose: Unified connection handling for all commands, MySQL driver integration
Output: Connection initializer, connection config struct, error handling
</objective>

<execution_context>
@C:/Users/xiaokexiang/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/xiaokexiang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-mysql-core-execution/01-CONTEXT.md
</context>

<interfaces>
<!-- No existing interfaces - greenfield project -->
</interfaces>

<tasks>

<task type="auto">
<name>Task 1: Create connection config and initializer</name>
<read_first>
- CLAUDE.md (GORM priority, no config storage)
- .planning/phases/01-mysql-core-execution/01-CONTEXT.md (decisions D-04 to D-09, D-25)
- .planning/REQUIREMENTS.md (CONN-01, CONN-02)
</read_first>
<action>
Create internal/database/connection.go with:

1. ConnectionConfig struct with fields:
   - Host string
   - Port int
   - User string
   - Password string
   - Database string
   - DBType string (mysql for now, dameng reserved)

2. NewConnectionConfig() function that returns empty ConnectionConfig

3. BuildDSN(cfg ConnectionConfig) (string, error) function that:
   - For MySQL: returns "user:password@tcp(host:port)/database?charset=utf8mb4&parseTime=True&loc=Local"
   - Returns error if required fields missing

4. OpenConnection(cfg ConnectionConfig) (*gorm.DB, error) function that:
   - Calls BuildDSN to get connection string
   - Opens GORM connection with mysql driver
   - Returns *gorm.DB and error

5. CloseConnection(db *gorm.DB) error function

Import paths:
- "gorm.io/gorm"
- "gorm.io/driver/mysql"
- "github.com/go-sql-driver/mysql"
</action>
<acceptance_criteria>
- internal/database/connection.go exists
- File contains "type ConnectionConfig struct"
- ConnectionConfig has Host, Port, User, Password, Database, DBType fields
- File contains "func BuildDSN(cfg ConnectionConfig) (string, error)"
- File contains "func OpenConnection(cfg ConnectionConfig) (*gorm.DB, error)"
- File contains "func CloseConnection(db *gorm.DB) error"
- MySQL DSN format includes charset=utf8mb4&parseTime=True&loc=Local
- File imports "gorm.io/driver/mysql"
</acceptance_criteria>
<verify>
<automated>grep -q "type ConnectionConfig struct" internal/database/connection.go && grep -q "func BuildDSN" internal/database/connection.go && grep -q "func OpenConnection" internal/database/connection.go</automated>
</verify>
<done>
Connection layer provides MySQL DSN building and GORM connection management
</done>
</task>

<task type="auto" tdd="true">
<name>Task 2: Write connection tests</name>
<read_first>
- internal/database/connection.go (created in Task 1)
</read_first>
<action>
Create internal/database/connection_test.go with tests:

1. TestBuildDSN_Success:
   - Input: ConnectionConfig{Host:"localhost", Port:3306, User:"root", Password:"pass", Database:"testdb", DBType:"mysql"}
   - Expected DSN: "root:pass@tcp(localhost:3306)/testdb?charset=utf8mb4&parseTime=True&loc=Local"

2. TestBuildDSN_MissingFields:
   - Input: ConnectionConfig with empty Host
   - Expected: error returned

3. TestBuildDSN_WithSpecialChars:
   - Password containing special chars like "p@ss:word"
   - Expected: properly escaped in DSN

4. TestOpenConnection_InvalidDSN:
   - Invalid host/port
   - Expected: error returned (connection refused)
</action>
<acceptance_criteria>
- internal/database/connection_test.go exists
- File contains "func TestBuildDSN_Success"
- File contains "func TestBuildDSN_MissingFields"
- File contains "func TestOpenConnection_InvalidDSN"
- Tests use table-driven approach or individual test functions
</acceptance_criteria>
<verify>
<automated>go test -v ./internal/database/... -run TestBuildDSN</automated>
</verify>
<done>
Connection tests verify DSN building and error handling
</done>
</task>

<task type="auto" tdd="true">
<name>Task 3: Implement connection logic after tests</name>
<read_first>
- internal/database/connection_test.go (test expectations)
- internal/database/connection.go (current implementation)
</read_first>
<action>
Run tests from Task 2 (RED phase - should fail), then implement:

1. BuildDSN validation:
   - Check Host, User, Password, Database not empty
   - Return error "host is required" if Host empty
   - Return error "user is required" if User empty
   - Return error "database is required" if Database empty
   - Default Port to 3306 if 0

2. BuildDSN formatting:
   - Use fmt.Sprintf for DSN construction
   - Format: "%s:%s@tcp(%s:%d)/%s?charset=utf8mb4&parseTime=True&loc=Local"

3. OpenConnection implementation:
   - Call gorm.Open(mysql.Open(dsn), &gorm.Config{})
   - Return db, err

4. CloseConnection implementation:
   - Get underlying *sql.DB via db.DB()
   - Call sqlDB.Close()
</action>
<acceptance_criteria>
- All tests in internal/database/connection_test.go pass
- `go test -v ./internal/database/...` exits 0
- BuildDSN returns error for missing Host, User, or Database
- BuildDSN produces exact DSN format for valid input
</acceptance_criteria>
<verify>
<automated>go test -v ./internal/database/...</automated>
</verify>
<done>
GREEN phase complete - all connection tests pass with implementation
</done>
</task>

</tasks>

<verification>
- All unit tests pass
- Connection can be established to test MySQL instance (if available)
- Error cases properly handled (missing fields, invalid host)
</verification>

<success_criteria>
- ConnectionConfig captures all required connection parameters
- BuildDSN produces valid MySQL DSN format
- OpenConnection returns working GORM instance
- Tests cover success and error cases
</success_criteria>

<output>
After completion, create .planning/phases/01-mysql-core-execution/01-02-SUMMARY.md with:
- Connection layer implementation notes
- Test coverage summary
- Any GORM/MySQL driver quirks discovered
</output>
