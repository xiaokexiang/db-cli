# Phase 7 Context

**Phase:** 7 — 跨仓库部署到 db-skill
**Milestone:** v1.1 CI/CD 自动化与跨仓库分发
**Created:** 2026-04-01

---

## Phase Goal

配置 PAT 跨仓库推送，更新 db-skill 仓库的二进制文件

**Requirements:**
- CI/CD-03: 自动更新 db-skill 仓库的二进制文件
- SEC-01: 配置跨仓库访问 PAT

---

## Implementation Decisions

### Target Repository

| Decision | Value |
|----------|-------|
| **Repository** | `xiaokexiang/db-skill` |
| **Checkout Method** | `actions/checkout@v4` with PAT |

### File Location

| Decision | Value |
|----------|-------|
| **Directory** | `scripts/` in db-skill repository |
| **Version Structure** | 直接覆盖（不保留历史版本） |
| **Files** | `db-cli-windows-amd64.exe`, `db-cli-windows-arm64.exe`, `db-cli-darwin-amd64`, `db-cli-darwin-arm64`, `db-cli-linux-amd64`, `db-cli-linux-arm64` |

### Deployment Trigger

| Decision | Value |
|----------|-------|
| **Approach** | 同一 workflow (release.yml) |
| **Job Type** | Add `deploy-to-db-skill` job after `create-release` |
| **Timing** | After release is published |

### Authentication

| Decision | Value |
|----------|-------|
| **Secret Name** | `CROSS_REPO_PAT` |
| **Token Type** | Fine-grained PAT or Classic PAT |
| **Required Scope** | `Contents: Read & Write` for db-skill repository only |
| **Location** | Stored in db-cli repository secrets |

---

## Existing Assets

| File | Status | Notes |
|------|--------|-------|
| `.github/workflows/release.yml` | Exists | Phase 6 重构后的 2-job workflow |
| `.planning/research/CROSS_REPO_DEPLOY.md` | Exists | 跨仓库部署研究报告 |

---

## Boundaries (Out of Scope)

- **PR 验证构建** — deferred to v1.1+
- **构建失败通知** — deferred to v1.1+
- **GitHub Packages 发布** — deferred to v1.1+
- **db-skill 仓库的其他修改** — 只更新 binaries 文件

---

## Technical Dependencies

### Phase 6 Dependency
Phase 7 依赖 Phase 6 的输出：
- `build` job 生成的 6 平台 binaries
- `create-release` job 创建的 Release

### Workflow Integration
在 release.yml 中添加 `deploy-to-db-skill` job：
```yaml
jobs:
  build:
    # Phase 6: builds binaries, uploads as artifacts
  
  create-release:
    # Phase 6: downloads artifacts, creates release, uploads assets
  
  deploy-to-db-skill:
    needs: create-release
    # Phase 7: downloads artifacts, pushes to db-skill/scripts
```

---

## Notes for Downstream Agents

### Researcher
- 已有 CROSS_REPO_DEPLOY.md 研究报告
- 确认 PAT 创建流程和权限要求
- 验证 actions/checkout 跨仓库检出配置

### Planner
- 用户选择直接覆盖 scripts/ 目录（无版本历史）
- 需要在 release.yml 中添加 deploy-to-db-skill job
- Secret `CROSS_REPO_PAT` 需要用户预先创建并配置

### Security Notes
- PAT 只需要 `Contents: R&W` 权限
- 限制到 `xiaokexiang/db-skill` 仓库
- 不要请求 `workflow` 权限（不需要修改 db-skill 的 workflows）

---

## Next Steps

**Research:** 验证 PAT 配置和 cross-repo checkout 细节
**Plan:** 创建实施计划
**Execute:** 配置 Secret → 修改 workflow → 端到端测试

---

*Generated by `/gsd:discuss-phase 7` workflow*
